You are advancing the simulation from {{ .CurrentDate }} to {{ .TargetDate }}.

Constraint profile: {{ .ConstraintProfile }}
Meaning: {{ .ConstraintMeaning }}

Output granularity: {{ .PromptDetailLevel }}
Meaning: {{ .DetailLevelInstruction }}

What you must do:
1. Infer the most important developments of the month from the current state plus the active directives.
2. Respect hard constraints, institutional friction, and path dependence.
3. Use the historical reference anchors as comparison material, not as an instruction to force history back onto the branch.
4. Emit only the most causally important changes that fit the requested granularity.
5. Keep the output compact enough to remain machine-reliable.

Current simulation state:
{{ .ProjectedState }}

Active directives for this month:
{{ .ActiveDirectives }}

Recent events already in the branch:
{{ .RecentEvents }}

Historical reference anchors for this month:
{{ .HistoricalAnchors }}

Continuity warnings from earlier review:
{{ .ContinuityWarnings }}

Output contract:
- Return exactly one RFC8259 JSON object.
- Do not wrap it in markdown.
- Do not add any commentary before or after the JSON.
- Keep the output within this granularity budget:
{{ .OutputBudget }}

Expected JSON shape:
{{ .JSONOutputExample }}

If the calling environment provides tools, use them instead of a freeform JSON reply.
If tools are not available, emit the JSON object directly in assistant content.
